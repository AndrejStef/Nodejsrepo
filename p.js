function personInformation (persons) {
    const information = `Person information: 
    ${persons.firstName} 
    ${persons.lastName} 
    ${persons.age} 
    ${persons.gender} 
    ${persons.ssn} 
    ${persons.country} 
    ${persons.city}`;

    return information;
}

function getLocation(){
    console.log(`${persons.country} ${person.city}`);
}
module.exports = {
    personInformation
}s